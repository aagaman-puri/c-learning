#include<stdio.h>
int main(){
    float m1 = 100; // chemestry marks 
    float m2 = 77;  // social marks 
    float m3 = 92; // english marks 
    float m4 = 90; // maths marks 
    float m5 = 86; // economics marks 
    float p = (m1 + m2 + m3 + m4 + m5)/5;
    printf ("percentage of 5 subjects is : %f",p);
    printf ("\n");
    float s1 = 34; // science marks 
    float s2 = 38; // maths marks 
    float s3 = 40; // english marks
    float s4 = 36; // nepali marks
    float tp = (s1 + s2 + s3 + s4)/4;
    printf("percentage of 4 subjects is : %f", tp);
    printf("\n");
    
    return 0;
}